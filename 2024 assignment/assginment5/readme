第1部分：创建个人资料
最新的斯坦福社交媒体创业公司是Treebook，你是团队的创始成员！为了让产品起飞并与一个未命名的、完全法律上无关的哈佛应用竞争，你被分配了实现用户个人资料的任务。
你将在user.cpp文件中编写User类。头文件user.h已为你提供并开始，但你需要向其添加特殊成员函数。

特殊成员函数：你需要在头文件中声明每个特殊成员函数，包括默认构造函数。默认构造函数应创建一个没有名字（空字符串）的用户个人资料。
请还创建一个额外的构造函数，该构造函数接受一个字符串名字，并使用此来更改名字私有变量。
类应该是可移动赋值和可移动构造的，但不是可复制赋值或可复制构造的。
第2部分：ABF：始终在结交朋友
现在User类已经创建，你已经开始为他们创建朋友了！你将实现两个操作符：+和<，以能够构建用户的朋友列表。

+操作符将表示将一个用户添加到另一个用户的朋友列表中。这应该是对称的，意味着例如将Charlie添加到Alice的朋友列表中，应该导致Alice也在Charlie的列表中。这应作为main.cpp中的非成员函数操作符重载实现。
<操作符对于在集合中存储用户是必需的，因为它们需要比较操作符。对此，我们将基于User名称的字母排序进行比较。这应作为成员函数实现。
就这些了！你可以运行main.cpp中的代码以确保没有编译错误。然后，将你的文件添加到Paperless。

